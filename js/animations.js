function animateLists(closed, listContainer) {

    if (closed) {
        console.log("opening");
        setTimeout(() => {
            if (x.classList.contains("hidden")) {
                x.classList.toggle("hidden");
            }
        }, time);
    }
}